import 'package:flutter/material.dart';

/// Pantalla placeholder de inicio de sesion.
///
/// La implementacion completa se realiza en el Sprint 3.
class LoginScreen extends StatelessWidget {
  /// Crea la pantalla placeholder de login.
  const LoginScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Login (Sprint 3)')));
  }
}
