import 'package:flutter/material.dart';

/// Pantalla placeholder de registro.
///
/// La implementacion completa se realiza en el Sprint 3.
class RegisterScreen extends StatelessWidget {
  /// Crea la pantalla placeholder de registro.
  const RegisterScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Registro (Sprint 3)')));
  }
}
